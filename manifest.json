{
  "manifest_version": 2,

  "name": "Jobbo",
  "description": "Chrome extension that notifies when a job position is published on a certain page.",
  "version": "0.1",

  "permissions": [
    "notifications",
    "alarms"
  ],

  "background": {
    "scripts": ["dist/scripts/main.js"],
    "persistent": true
  }
}